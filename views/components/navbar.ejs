<nav class="container-xl p-2 d-flex gap-2 align-items-center">
  <a href="/" class="navbar-brand fs-2 fw-bold">
    📖 BookApp
  </a>
  <div class="d-flex flex-grow-1 justify-content-center align-items-center">
  </div>
  <div class="ms-auto d-flex gap-3">
    <%
      if (!selfUser) {
    %>
    <a href="/register" class="btn btn-outline-success">Register</a>
    <a href="/login" class="btn btn-success">Login</a>
    <% } else { %>
    <p class="d-inline-block m-0 my-auto" style="font-size: 14px">
      Hi,
      <a href="#" class="custom-link text-success">
        <%= selfUser.full_name.slice(0, 32) %>
      </a>
    </p>
    <a href="/logout" class="btn btn-outline-success">Logout</a>
    <% } %>
  </div>
</nav>